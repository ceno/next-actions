{
  "name": "next-actions",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Trello Power-Up: always-on checklist next-action badges on the card front.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite preview",
    "host": "bash scripts/host.sh"
  },
  "devDependencies": {
    "fast-check": "^3.23.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vitest": "^3.0.5"
  }
}
